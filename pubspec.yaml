name: family_finance
description: Ứng dụng quản lý tài chính gia đình đa nền tảng.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.5.1
  google_fonts: ^6.2.1
  intl: ^0.20.2
  fl_chart: ^0.68.0
  table_calendar: ^3.1.2
  firebase_core: ^3.2.0
  firebase_auth: ^5.1.2
  cloud_firestore: ^5.2.0
  google_sign_in: ^6.2.1
  local_auth: ^2.2.0
  flutter_local_notifications: ^17.2.1
  timezone: ^0.9.4
  firebase_storage: ^12.1.0
  image_picker: ^1.1.2
  cached_network_image: ^3.3.1
  flutter_image_compress: ^2.4.0
  uuid: ^4.1.0
  connectivity_plus: ^6.0.3
  shared_preferences: ^2.3.0
  share_plus: ^10.0.0
  pdf: ^3.10.8
  printing: ^5.12.0
  package_info_plus: ^8.0.0
  # ── Location Tracking (Feature XII) ──────────────────────────────
  google_maps_flutter: ^2.9.0
  geolocator: ^13.0.2
  permission_handler: ^11.3.1
  geocoding: ^3.0.0
  battery_plus: ^6.0.1
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
